# Tech Insight 플랫폼 의존성
# 설치: pip install -r requirements.txt

# 웹 프레임워크
Django==6.0.5

# PDF 본문 추출 (2단 편집 대응 파이프라인)
pdfplumber==0.11.9

# 운영 서버용 WSGI 서버 (개발 runserver 대신)
gunicorn==23.0.0

# 운영 모드에서 정적파일(CSS/JS) 서빙
whitenoise==6.8.2

# 엑셀(.xlsx) 읽기 — Cowork 추출 결과 적재용
openpyxl==3.1.5

# 뉴스 자동 수집 (RSS 파싱 + 본문 크롤링)
feedparser==6.0.12
requests==2.34.2
beautifulsoup4==4.15.0

# 수치 연산(검색 임베딩 유사도)
numpy==2.5.0

# .env 로딩
python-dotenv==1.2.2
